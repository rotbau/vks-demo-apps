apiVersion: v1
kind: Service
metadata:
  name: webapp-color-service
  namespace: default
spec:
  type: ClusterIP
  selector:
    app: webapp-color
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
